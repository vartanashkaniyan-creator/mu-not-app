# 🏗️ نرم‌افزارساز حرفه‌ای

این پروژه یک کتابخانه و موتور اپ‌ساز حرفه‌ای است که امکان تولید اپلیکیشن‌های موبایل، وب و دسکتاپ را به صورت همزمان فراهم می‌کند.

## ویژگی‌ها

- پشتیبانی از موبایل، وب و دسکتاپ
- مدیریت یادداشت‌ها، ماشین حساب، لیست کارها و ۲۵ اپ دیگر
- ذخیره‌سازی آفلاین و آنلاین
- طراحی قابل توسعه و به‌روزرسانی
- پشتیبانی از چند زبان (فارسی و انگلیسی)
- ساختار کلاس محور، MVC، مدیریت خطا و ثبت لاگ
- خروجی HTML، CSS، JS آماده اجرا

## ساختار فایل‌ها

1. `app.library.js` – کتابخانه اصلی اپ‌ها  
2. `app.js` – مدیریت و راه‌اندازی اپ‌ها  
3. `calculator.js` – اپ ماشین‌حساب پیشرفته  
4. `core.js` – هسته اصلی موتور  
5. `engine.js` – موتور پردازش و اجرای اپ‌ها  
6. `i18n.js` – مدیریت چندزبانه  
7. `index.html` – رابط کاربری اصلی  
8. `language.switcher.js` – سوئیچر زبان  
9. `main.js` – کنترل اصلی برنامه  
10. `manifest.json` – تنظیمات PWA  
11. `notes.html` – رابط یادداشت‌ها  
12. `notes.js` – مدیریت یادداشت‌ها  
13. `preview.html` – پیش‌نمایش اپ  
14. `preview.js` – مدیریت پیش‌نمایش  
15. `router.js` – مدیریت مسیرها  
16. `script.js` – اسکریپت‌های کمکی  
17. `service.worker.js` – سرویس‌ورکر  
18. `storage.js` – ذخیره‌سازی داده‌ها  
19. `style.css` – استایل‌ها  
20. `templates.js` – قالب‌های آماده  
21. `ui.js` – رابط کاربری و ناوبری  
22. `calculator.app.js` – نسخه حرفه‌ای ماشین حساب  

## شروع

۱. فایل‌ها را در یک پوشه قرار دهید.  
۲. `index.html` را در مرورگر باز کنید.  
۳. از طریق رابط کاربری، اپ مورد نظر خود را بسازید و خروجی دریافت کنید.  

## توسعه

- همه کلاس‌ها و اپ‌ها کلاس محور هستند.  
- امکان اضافه کردن اپ‌های جدید با ساخت کلاس جدید و ثبت در `AppFactory` وجود دارد.  
- تغییر زبان، تم، ذخیره خودکار و حالت آفلاین به راحتی قابل فعال/غیرفعال کردن هستند.  

## پشتیبانی

- برای گزارش باگ یا درخواست امکانات جدید، به فایل `issues.md` مراجعه کنید.  

---
نسخه ۳.۰.۰ – ۲۰۲۶/۰۱/۰۷
